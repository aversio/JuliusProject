@model IEnumerable<MVC_CodeFirst_Login.Models.Consult>
@{
    ViewData["Title"] = "Consult";
}

<div class="row">
    <div class="col-md-12">
        <h2>Consult</h2>
        @if (Model.Any())
        {
            <table class="table">
                <tr>
                    <th>BeginDate</th>
                    <th>EndDate</th>
                </tr>
                @foreach (var item in Model)
                {
                <tr>
                    <td>@item.BeginDate</td>
                    <td>@item.EndDate</td>
                </tr>
                }
            </table>
        }
        else
        {
            <p>there are no regisstered users</p>
        }
    </div>

    <p>
        <a asp-action="Create">Maak consult</a>
    </p>
</div>